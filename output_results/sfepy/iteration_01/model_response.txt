```bash
# Install dependencies from requirements files if present.
if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
if [ -f pyproject.toml ]; then pip install .; fi
if [ -f setup.py ]; then pip install .; fi

# Now attempt to run pytest. If this errors with missing modules, will fix next.
pytest
```